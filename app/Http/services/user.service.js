app.service('UserService', function ($http, API_URL){

    // var page_type = "";
	// this.getPageType = function(id, pageTitle){

	// 	return $http.get(API_URL + '/api/users/' + id + '/' + pageTitle).success(function (response){
			
	// 		if(response)
	// 			return( this.page_type = "edit");
	// 		else
	// 			return this.page_type = "add";
	// 	})
        
	// }

});
